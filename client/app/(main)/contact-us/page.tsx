import Box from "@/components/ui/Box";
import { Typography } from "@/components/ui/Typoragphy";

const ContactUsPage = () => {
  return (
    <Box>
      <Typography variant="h3">ContactUs</Typography>
    </Box>
  );
};

export default ContactUsPage;
